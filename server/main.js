import { Meteor } from 'meteor/meteor';
import '../imports/bundles/ListBundle/Model/Nendoroids';
import '../imports/bundles/AuthBundle/Model/Otakus';

Meteor.startup(() => {
  // code to run on server at startup
});
